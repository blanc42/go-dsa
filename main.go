package main

import "dsa/algodaily"

func main() {
	// fmt.Println(algodaily.StringReverse("something is bad"))
	// b := []int{-1, 0, 11, 6, 2, 12}
	// sort.BubbleSort(b)
	// fmt.Println(b)
	// a := []int{11, 12, 13, 5, 6}
	// fmt.Println(leetcode.BinarySearch(a, 3))
	// fmt.Println(leetcode.GuessNumber(16))
	// fmt.Println(searchInsert(a, 5))
	// fmt.Println(a)
	// algos.MergeSort(a)
	// fmt.Println(a)
	// fmt.Println(algodaily.ArraryIntersection(a, b))
	// x := new(ds.LinkedList)
	// x.Prepend(&ds.Node{Data: 11, Next: nil})
	// x.AddNode(&ds.Node{Data: 12, Next: nil})
	// x.AddNode(&ds.Node{Data: 13, Next: nil})
	// x.AddNode(&ds.Node{Data: 14, Next: nil})
	// x.Print()

	// Graph
	// g := graph.NewGraph()
	// g.AddVertex(1)
	// g.AddVertex(2)
	// g.AddEdge(1, 2, 2)
	// g.AddVertex(3)

	// g.AddEdge(3, 2, 4)
	// g.AddVertex(4)
	// g.AddEdge(1, 4, 6)
	// g.AddEdge(4, 1, 3)
	// g.Print()
	algodaily.CheckFirstNonRepeating()

}
